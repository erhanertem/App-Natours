//- pug is indentation sensitive
//- Pug can also understand HTML code here we show PUG syntax to get familiar with
//- pug syntax representation of html syntax <h1>The Park Camper</h1>
//- This kind of commenting appears on the output code
//- Buffered commenting: This kind of commenting do not appear on the final code. 

doctype html
//- html(lang='en') 
html 
  head
    block head
      meta(charset='UTF-8')
      meta(name='viewport' content='width=device-width, initial-scale=1.0')
      title Natours | #{title}
      //- title Natours #{tour} --> #{...} is used as template literals in pug
      //- SERVING STATIC FILES in public folder by express middleware @ app.js tells where css and img folders are
      link(rel='stylesheet' href='/css/style.css')
      link(rel='shortcut icon' type='image/png' href='/img/favicon.png')
      link(rel='stylesheet' href='https://fonts.googleapis.com/css?family=Lato:300,300i,700')

  body
    // HEADER
    include _header
    //- imports the code from the reference file. 
    // CONTENT
    block content
      h1 This is a placeholder heading
    //FOOTER
    include _footer
    //- //JS SCRIPT REFERENCE
    //- script(src='/js/mapbox.js')