header.header
  nav.nav.nav--tours
    a.nav__el(href='/') All tours
    form.nav__search
      button.nav__search-btn
        svg
          use(xlink:href='/img/icons.svg#icon-search')
      input(type='text'
        placeholder='Search tours'
        class='nav__search-input')
  .header__logo
    img(src='/img/logo-white.png' alt='Natours logo')
  nav.nav.nav--user
    //-@authContoller.js snippet- res.locals.user = currentUser; allows pug files get assigned variables - this is how we got user
    if user
      a.nav__el.nav__el--logout Log out
      a.nav__el(href='#') 
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
        span= user.name.split(' ')[0] 
          //-Get the first name only
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href='#') Sign up 
        //-While Sign up process is similar to login process in the scope of this course it won't be coded.